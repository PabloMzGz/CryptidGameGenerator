/**
 * sharing.js — Game code encoding/decoding & URL parameter handling.
 *
 * Phase 4 placeholder.  This module will implement:
 *   - Compact hex-string game codes (see CLAUDE.md §Sharing System)
 *   - URL parameter parsing (?game=CODE&player=N&lang=xx)
 *   - Player-specific URL generation
 *   - Sharing UI (copy-to-clipboard, per-player links)
 *
 * No implementation yet — added as an empty module so the script tag order
 * in index.html is established for future work.
 */
